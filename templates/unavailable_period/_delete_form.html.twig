{% extends 'base.html.twig'%}
{% block body %}

<form method="post" action="{{ path('unavailable_period_delete', {'id': unavailable_period.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unavailable_period.id) }}">
    <button class="btn">Delete</button>
</form>
{% endblock %}
